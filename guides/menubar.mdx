---
title: "Menubar App"
icon: "display"
description: "Monitor routing, savings, and status from your Mac menu bar."
---

The Rayline menubar app gives you a glanceable overview of your routing activity — and a one-click way to launch Claude Code — without opening the full dashboard. It lives in your macOS menu bar and shows live data.

<Frame caption="The Rayline menubar popover.">
  <img src="/images/menubar-popover.png" alt="Rayline menubar popover showing savings, a Launch Claude Code button, and routing controls" />
</Frame>

## Installation

### Automatic (recommended)

If you use the `rl` CLI, the menubar app is installed automatically on first run of `rl claude` on macOS (Apple Silicon).

### Manual

```bash
rl menubar install
```

This downloads the latest Rayline build and installs it to `/Applications`.

### Starting and stopping

```bash
rl menubar start    # launch the menubar app
rl menubar stop     # quit the menubar app
```

Or just launch Rayline from `/Applications` like any Mac app.

## What it shows

Click the Rayline icon in your menu bar to open the popover:

### Launch Claude Code

The primary action (⌘↩) — one click starts a routed Claude Code session in a new Terminal window, running `rl claude` for you. Below it, **Copy launch command** (⌘C) copies the command to paste anywhere, and **Install command line tools** symlinks `rl` and `rld` into `/usr/local/bin` so they're on your PATH.

### Cost savings

A summary of your routing savings at the top of the popover — the amount saved over Today / 7 days / 30 days, and how much cheaper that is versus running everything on a frontier model. Same counterfactual calculation as the dashboard, updated in near-real-time.

### Routing controls

- **Main model** — pin the main agent to a specific model, or leave it on **Dynamic** to let the router decide.
- **Activity** — recent routed requests with model, tokens, cache, latency, cost, and savings.
- **Routing config** — switch between ML and rules-based routing and toggle on-device routing.

### Account

The footer shows your signed-in email and a quit button, and **Open dashboard** jumps to the full router dashboard in your browser.

## Authentication

The menubar app uses the same credentials as the `rl` CLI. If you've run `rl auth login`, it picks up those credentials automatically from `~/.config/rayline/credentials.json`.

If you see "Not logged in" in the menubar, click the **Login** button in the popover (it runs `rl auth login` for you), or run it yourself:

```bash
rl auth login
```

## Auto-start

The menubar app can be configured to start with your Mac:

```bash
rl menubar autostart on    # start on login
rl menubar autostart off   # don't start on login
```

## Opting out

If you don't want the menubar app to auto-install when running `rl claude`:

```bash
export RAYLINE_MENUBAR_OPT_OUT=1
```

Add this to your `~/.zshrc` to make it permanent.

## Requirements

- macOS 13+ (Ventura or later)
- Apple Silicon (M1 or later) for auto-install; Intel Macs can use manual install if a build is available
